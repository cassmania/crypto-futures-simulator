# Crypto Futures Simulator V4.1 이식 계획

## 목표

- 기존 차트, 주문, 자동체결, 포지션 및 시뮬레이션 로직을 변경하지 않는다.
- 기존 확정봉·멀티 타임프레임 레벨·Binance Futures 데이터를 재사용해 코인분석스킬 V4.1 PART 0~5 보고서를 추가한다.
- 수집하지 않은 온체인·고래·청산맵·뉴스·언락 데이터는 추정하지 않고 `현재 실시간 데이터 확인 불가`로 표시한다.

## 구현 단계

1. 변경 대상 파일을 지정 백업 폴더에 복사한다.
2. 독립 `v41_analysis.js` 모듈에서 SMA 20/60/120/200, RSI(14), CCI(20), MACD, Stochastic(14,3,3), 앵커 VWAP, 근사 VPVR을 확정봉으로 계산한다.
3. 기존 멀티 타임프레임 레벨과 Binance 펀딩비·OI를 V4.1 보고서에 연결한다.
4. Upbit KRW-USDT 환율과 CoinGecko USDT.D를 출처·조회 시각과 함께 표시한다.
5. 퀀트 지표 탭에 PART 0~5 보고서 패널을 추가한다.
6. 손익비 1.5 미만에서는 진입 전략을 관망으로 제한하고 계좌 위험액 기반 포지션 크기 산식을 표시한다.
7. 기존 및 신규 테스트, 문법 검사, 모바일 브라우저 검증을 수행한다.
8. 관련 파일만 커밋·푸시하고 GitHub Pages 배포 완료와 공개 화면을 확인한다.

## 완료 기준

- 기존 무결성·레벨·파동 테스트와 V4.1 신규 테스트가 모두 통과한다.
- 390×844 화면과 공개 URL에서 PART 0~5가 정상 출력되고 브라우저 오류가 없다.
- 자동매매·주문 관련 파일의 동작 변경이 없다.
