# 선물 시뮬레이터 지표 백테스트·정확도 개선 계획

## 목표

현재 사이트가 실제로 발생시키는 다중 지표 LONG·SHORT 신호를 Binance USDT-M 확정봉으로 재현하고, 미래 데이터 참조 없이 비용 포함 홀드아웃 성과를 측정한다. 검증 가능한 오류만 최소 범위로 수정하고, 근거가 재현되지 않는 과거 성과 문구는 검증 상태를 명확히 표시한다.

## 현재 확인한 오류·위험

1. MA 데드크로스가 `현재 EMA5 <= 현재 EMA20`이 아니라 `현재 EMA5 <= 이전 EMA20`으로 계산된다.
2. 가격 변동이 전혀 없는 RSI 구간에서 평균 상승과 평균 하락이 모두 0인데 RSI 100으로 표시된다. 표준 중립값은 50이다.
3. 모의 포지션 진입 시 시장가 수수료를 차감하지 않고, 종료 이력의 실현손익도 종료 수수료 차감 전 금액을 기록한다.
4. 신호는 확정봉 지표와 실시간 가격을 섞어 사용한다. 화면 관찰에는 가능하지만 역사 백테스트에서는 신호 확정봉 종가와 다음 봉 시가를 엄격히 분리해야 한다.
5. `wave_analyzer.js`의 8,778건, 워크포워드 69.6%, 심볼 홀드아웃 76.5% 수치는 저장소에 재현 스크립트·원본 결과가 없어 현재 공개 코드만으로 검증할 수 없다.

## 구현 범위

1. MA 데드크로스와 횡보 RSI 계산을 수정하고 기준값 테스트를 추가한다.
2. 진입·종료 수수료를 모두 지갑 잔고와 거래 이력의 순손익에 반영한다.
3. 현재 다중 지표 신호 규칙을 브라우저와 Node에서 함께 쓰는 순수 함수 모듈로 분리한다.
4. Binance 서버 시각을 기준으로 진행 중인 봉을 제거하고, 최대 5,000개 확정봉을 페이지네이션한다.
5. 마지막 40% 시간 구간을 홀드아웃으로 고정한다. 신호는 현재 확정봉까지만 계산하고 진입은 다음 봉 시가로 한다.
6. 기본 자동 TP·SL 규칙을 재현하고 한 봉에서 목표와 손절이 모두 닿으면 손절을 우선한다.
7. 진입·청산 각각 수수료 0.04%와 슬리피지 0.02%를 적용한다.
8. 거래 수, 승률, Profit Factor, 평균 순손익 R, 누적 R, 최대낙폭 R을 산출하며 30거래 미만은 `표본 부족`으로 표시한다.
9. BTCUSDT·ETHUSDT·SOLUSDT의 1시간·4시간봉을 동일 조건으로 검증하고 JSON·마크다운 보고서를 남긴다.
10. 화면에 현재 종목·시간봉 백테스트를 실행하는 읽기 전용 패널을 추가한다. 주문·자동매매 상태는 변경하지 않는다.
11. 재현 근거가 없는 파동 성과값은 삭제하지 않되 `과거 내부 참고값·현재 재현 자료 없음`으로 명확히 제한한다.

## 성공 기준

1. 기존 데이터 무결성·파동·지지저항·V4.1 테스트가 모두 통과한다.
2. 신규 지표·비용·미래참조·동시 도달 테스트가 통과한다.
3. 6개 실데이터 홀드아웃 결과가 생성되고 표본 부족 여부가 구분된다.
4. 390×844 모바일 화면에서 가로 넘침과 브라우저 오류가 없다.
5. GitHub Pages 공개본에서 최신 확정봉 분석과 백테스트 버튼이 실제로 동작한다.

