# 활성 포지션 한눈 요약 구현 계획

## 목표

현재 화면은 활성 포지션 개수만 상단에 보이고, 개별 포지션은 화면 아래쪽의 긴 표에서만 확인할 수 있다. 상단의 `활성 포지션` 영역을 눌렀을 때 현재 화면을 떠나지 않고 핵심 위험과 성과를 즉시 파악할 수 있는 요약 패널을 제공한다.

## 적용 방식

1. 상단 `활성 포지션` 항목을 접근 가능한 버튼으로 변경한다.
2. 버튼 바로 아래에 펼쳐지는 `포지션 한눈 요약` 패널을 추가한다.
3. 패널 상단에는 전체 포지션 수, LONG/SHORT 수, 수익/손실 포지션 수, 총 미실현 손익을 표시한다.
4. 포지션 목록은 위험 우선순위로 정렬한다.
   - 청산가까지 거리가 가까운 포지션을 먼저 표시한다.
   - 각 행에는 코인, 방향, 손익, 수익률, 청산가까지 거리를 표시한다.
   - 손익과 위험도는 색상 및 상태 문구를 함께 사용해 색상만으로 판단하지 않게 한다.
5. 패널의 `하단 상세표 보기` 버튼으로 기존 활성 포지션 표까지 즉시 이동한다.
6. 바깥 영역 클릭과 `Esc` 키로 패널을 닫을 수 있게 한다.
7. 모바일에서는 화면 폭에 맞는 카드형 패널로 표시한다.

## 안전 범위

- 주문 생성, 정산, 자동 마진, 손익 계산식은 변경하지 않는다.
- 기존 활성 포지션 표와 로컬 저장 데이터 구조는 그대로 유지한다.
- 요약 패널은 기존 `상태.활성포지션` 값을 읽어서 표시만 한다.

## 검증 기준

- 포지션 0개/다수 상태에서 오류 없이 렌더링된다.
- 상단 버튼, 바깥 클릭, `Esc`, 상세표 이동이 작동한다.
- 실시간 가격 갱신 시 손익과 청산 거리 요약도 함께 갱신된다.
- 데스크톱과 모바일 화면에서 패널이 화면 밖으로 넘치지 않는다.
- JavaScript 구문 검사와 실제 브라우저 동작 검증을 통과한다.
- 배포 후 공개 URL에서 새 UI와 동작을 다시 확인한다.
