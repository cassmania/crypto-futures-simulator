# 구현 계획 — 데이터 정확도·안정성 업그레이드

## 목표

공개 페이지가 실제 Binance 선물 데이터와 내부 계산값을 명확히 구분하도록 바꾸고, 미완성 봉·혼합 시간봉·중복 API 호출 때문에 분석 결과가 흔들리는 문제를 줄인다.

## 확인된 문제

1. 펀딩비, 미결제약정, 롱·숏 비율, 청산맵, 고래 유입이 실제 원천 데이터가 아닌 계산 또는 추정값인데 실시간 값처럼 표시된다.
2. DXY 수집 실패 시 고정 기준값과 임의 랜덤워크를 실제 수치처럼 표시한다.
3. 저항·지지 패널의 `정밀도 99%`는 백테스트 적중률이 아니라 내부 수렴 점수인데 정확도처럼 표현된다.
4. 미완성 현재 봉이 RSI·MACD·VPVR·엘리엇 파동 분석에 포함된다.
5. 1분 웹소켓 봉이 코인별로 보관된 1시간·4시간·일봉 배열에 섞일 수 있다.
6. CME 갭 계산과 DXY 요청이 지나치게 자주 반복되어 API 부하와 콘솔 오류를 만든다.
7. 코인·시간봉을 빠르게 바꾸면 이전 요청 응답이 새 선택을 덮어쓸 수 있다.

## 구현 순서

### 1단계 — 데이터 무결성

- REST 캔들에 종료 시각과 확정 여부를 저장한다.
- 분석용 데이터는 확정 봉만 사용한다.
- 실시간 1분 봉 버퍼를 차트 시간봉 데이터와 분리한다.
- 차트 요청마다 식별 번호를 부여해 늦게 도착한 이전 응답을 폐기한다.
- API 실패 시 가상 캔들을 분석 데이터로 사용하지 않고 화면에 `데이터 없음`을 표시한다.

### 2단계 — 실제 파생상품 데이터

- Binance Futures 공개 API에서 실제 펀딩비, 미결제약정, 글로벌 계정 롱·숏 비율을 가져온다.
- 값마다 출처와 갱신 시각을 함께 표시하고 60초 캐시를 적용한다.
- 공개 데이터가 없는 MVRV/SOPR, 고래 순유입, 청산맵은 임의 추정하지 않고 `데이터 없음`으로 표시한다.

### 3단계 — 오해 소지 제거와 호출 최적화

- 백테스트 적중률처럼 보이는 `정밀도/신뢰도 %`를 `수렴 점수/근거 강도`로 바꾼다.
- DXY 실패 시 랜덤값을 만들지 않고 `데이터 없음`으로 표시한다.
- DXY는 5분, CME 갭은 5분 캐시로 제한한다.
- 시장 제목과 데이터 출처를 Binance Futures 기준으로 통일한다.

### 4단계 — 검증과 배포

- 기존 파동·지지저항 테스트와 신규 데이터 무결성 테스트를 실행한다.
- 브라우저에서 코인/시간봉 전환, 네트워크 요청 빈도, 콘솔 오류, 출처 표기를 확인한다.
- `main`에 커밋·푸시하고 GitHub Pages 공개본의 배포 버전과 동작을 다시 확인한다.

## 변경 예정 파일

- `app.js`: 데이터 수집, 확정 봉, 요청 경쟁 방지, 파생 데이터, DXY/CME 캐시
- `index.html`: 출처·데이터 없음·점수 명칭·배포 버전 표기
- `test_data_integrity.js`: 확정 봉과 시간봉 분리 회귀 테스트

## 백업 기준점

- 원본 커밋: `8655fc07bc3dfc069eaab3a0645f99a3c3d0400d`
- GitHub 백업 브랜치: `codex/backup-pre-upgrade-2026-08-17`
- 로컬 백업: `E:\AI 관련 자료\챗지피티\backups\crypto-futures-simulator\2026-08-17_원본_업그레이드전_8655fc0`

## 구현·검증 결과

- 확정 봉 필터와 신호용 1분봉 전용 버퍼를 적용했다.
- 코인·시간봉 연속 전환 시 이전 응답을 폐기하고, 같은 코인의 서로 다른 시간봉을 클릭해도 해당 차트 데이터로 분석이 동기화되는 것을 확인했다.
- Binance Futures 실제 펀딩비·미결제약정·5분 글로벌 롱숏 계정 비율 응답을 브라우저에서 확인했다.
- 온체인, 고래 지갑, 청산맵, CME는 원천 미연동 상태를 `데이터 없음`으로 표시한다.
- DXY는 공개 환율 바스켓 근사치임을 명시하고 신호 점수에서 제외했다.
- Node 문법 검사, 데이터 무결성 테스트, 파동 테스트, 지지저항 31개 테스트를 통과했다.
- 로컬 브라우저 콘솔에서 애플리케이션 JavaScript 오류가 없음을 확인했다.
